{
  "name": "gitacp",
  "version": "1.0.0-beta1",
  "description": "gitacp",
  "main": "./es/index.js",
  "module": "./es/index.js",
  "typings": "./es/index",
  "files": [
    "es"
  ],
  "bin": {
    "gitacp": "./es/bin/gitacp"
  },
  "devDependencies": {
    "typedoc": "^0.9.0",
    "typescript": "^2.6.1",
    "fs-extra": "^5.0.0"
  },
  "scripts": {
    "clean": "node ./tools/clean.js",
    "tsBuild": "tsc -p tsconfig.json",
    "docs": "typedoc --out docs src --module commonjs --hideGenerator && node ./tools/formatDocs.js",
    "build": "npm run clean && npm run tsBuild",
    "start": "npm run build && node ./es/index.js"
  },
  "keywords": [
    "gitacp"
  ],
  "author": "heifade",
  "license": "MIT",
  "dependencies": {
    "@types/commander": "^2.12.2",
    "child_process": "^1.0.2",
    "commander": "^2.12.2"
  }
}
