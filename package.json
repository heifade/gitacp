{
  "name": "gitacp",
  "version": "1.0.20",
  "description": "gitacp",
  "main": "./es/index.js",
  "module": "./es/index.js",
  "typings": "./es/index",
  "files": [
    "es"
  ],
  "bin": {
    "gitacp": "./es/bin/gitacp",
    "gitp": "./es/bin/gitacp",
    "acp": "./es/bin/gitacp",
    "pp": "./es/bin/gitacp"
  },
  "scripts": {
    "clean": "node ./tools/clean.js",
    "code-build": "tsc -p tsconfig.json && node ./tools/copy.js",
    "docs": "typedoc --out docs src --module commonjs --hideGenerator && node ./tools/formatDocs.js",
    "build": "npm run clean && npm run code-build && npm run docs",
    "start": "npm run clean && npm run code-build && node ./es/index.js -v"
  },
  "keywords": [
    "gitacp"
  ],
  "author": "heifade",
  "license": "MIT",
  "devDependencies": {
    "@types/yargs": "^10.0.0",
    "fs-extra": "^5.0.0",
    "typedoc": "^0.9.0",
    "typedoc-format": "^1.0.2",
    "typescript": "^2.6.1"
  },
  "dependencies": {
    "chalk": "^2.3.0",
    "child_process": "^1.0.2",
    "fs-i": "^1.0.3",
    "yargs": "^10.0.3"
  }
}